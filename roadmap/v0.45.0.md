# v0.45.0 — Operational Readiness, Scalability & CI Completeness

> **Full technical details:** [v0.45.0.md-full.md](v0.45.0.md-full.md)

**Status: ✅ Released** | **Scope: Large**

> Fix Dockerfile VERSION drift, add container HEALTHCHECKs, ship production
> CNPG/monitoring examples, implement preflight and worker-status functions,
> make invalidation ring and cost-cache capacity configurable, add WAL slot
> preflight, harden cross-platform CI, add full-image PR smoke tests, restore
> E2E coverage schedule, and persist Citus failure counters.

---

## What is this?

The assessment found that operational deployment assets are demo-grade rather
than production-ready: Dockerfiles use stale VERSION defaults, containers lack
HEALTHCHECKs, CNPG examples underprovision workers, worker exhaustion is only
visible through logs, shared-memory capacities are hardcoded, WAL slot capacity
is reactive, cross-platform CI is non-blocking, and full E2E coverage is not
continuously measured. v0.45.0 closes all of these gaps.

---

## Container and deployment hardening

Dockerfile VERSION args are synced to Cargo.toml at build time. All runtime
images get `pg_isready` HEALTHCHECKs. CNPG ships separate dev and production
examples with explicit worker budgets, resource requests, WAL sizing, and
monitoring hooks.

---

## Preflight and runtime status functions

`pgtrickle.preflight()` checks worker slots, replication slots, WAL level,
shared_preload_libraries, and scheduler state. `pgtrickle.worker_pool_status()`
exposes pool size, active workers, queued jobs, and blocked reasons. Both are
SQL-callable so operators have runtime visibility without log scraping.

---

## Scalability infrastructure

The invalidation ring capacity (currently 128) becomes preload-configurable
with overflow metrics. Worker-slot exhaustion becomes SQL-visible status.
WAL slot capacity is checked proactively before enabling WAL/auto mode. Citus
failure counters persist across scheduler restarts.

---

## CI completeness

Cross-platform compile failures become blocking on scheduled runs. A small
required full-image smoke test runs on PR/push. E2E coverage schedule is
restored. Weekly E2E coverage tracks module-level changes.

---

## Also in v0.45.0

- Incremental DAG rebuild optimization for frequent DDL
- Lag-aware cross-database scheduling
- Storage Backends reference page
- dbt macro option sync with Rust options struct
- Production monitoring split (demo vs production guidance)

---

## Scope

v0.45.0 is the largest assessment-driven release because it spans Docker, CNPG,
shared memory, CI workflows, scalability features, and operational SQL APIs. It
is the final version in the assessment hardening arc before the roadmap resumes
the embedding programme.

> **Source:** [plans/PLAN_OVERALL_ASSESSMENT_9.md](https://github.com/trickle-labs/pg-trickle/blob/5ed05167/plans/PLAN_OVERALL_ASSESSMENT_9.md)
> findings OPS-01 through OPS-06, SCAL-01 through SCAL-07, CI-01 through CI-03,
> TEST-07, TEST-08, FEAT-03, FEAT-06.

---

*Previous: [v0.44.0 — Security Hardening & Code Quality](v0.44.0.md)*
*Next: [v0.46.0 — Embedding Pipeline Infrastructure & ANN Maintenance](v0.46.0.md)*
