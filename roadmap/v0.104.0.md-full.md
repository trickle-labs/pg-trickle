# v0.104.0 full implementation details

**Status: Released.** v0.104.0 freezes the public Graph V1 and Delta V1 SQL
contracts after a focused conformance suite.

## Delivered

- Stable capability discovery for Graph V1 and Delta V1.
- Public-SQL reference coordinator and output-delta consumer tests.
- Transactional rollback and publication coverage for graph refresh.
- Exact output-delta batch, typed relation, cursor acknowledgement, and
  resnapshot coverage.
- External graph provenance in refresh history and output-delta batches.
- Release archive, upgrade migration, capability manifest, and release gate.

## Verification

The v0.104 release gate checks package versions, generated SQL, stable
capability declarations, conformance-suite inventory, documentation, and
roadmap status. Runtime conformance runs in `just test-v104-conformance`.
