[graph]
all-features = true

[licenses]
# Allow the standard permissive and weak-copyleft open-source licenses used
# across the dependency tree. Unicode-3.0 is required by the icu_* crates;
# BSL-1.0 is used by Boost-derived crates pulled in by pgrx internals.
version = 2
allow = [
  "Apache-2.0",
  "MIT",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "ISC",
  "Unlicense",
  "Zlib",
  "BSL-1.0",
  "Unicode-3.0",
]

[advisories]
version = 2
yanked = "warn"
ignore = [
  # Owner: geir-gronmo
  # Added: 2026-05-22
  # Review-By: 2026-08-22
  # Upstream: https://github.com/dtolnay/paste/issues/108
  # Remove-When: pgrx updates paste dependency to a maintained alternative
  # paste is used internally by pgrx macros; we have no upstream control over
  # this dep. Not a vulnerability, only a maintenance status notice.
  "RUSTSEC-2024-0436",
  # Owner: geir-gronmo
  # Added: 2026-05-22
  # Review-By: 2026-08-22
  # Upstream: https://github.com/rustls/rustls/issues/2104
  # Remove-When: bollard/testcontainers updates to rustls-pki-types
  # rustls-pemfile v2 is pulled in by testcontainers/bollard (dev-only path).
  # Maintenance moved to rustls-pki-types; will be resolved when bollard updates.
  "RUSTSEC-2025-0134",
  # Owner: geir-gronmo
  # Added: 2026-05-22
  # Review-By: 2026-08-22
  # Upstream: https://github.com/pyfisch/cbor/issues/172
  # Remove-When: transitive dep chain updated to use ciborium
  # serde_cbor is pulled in transitively (dev path). Not a security vulnerability,
  # only unmaintained. Use of ciborium is the upstream recommendation but we
  # have no direct control over this dep.
  "RUSTSEC-2021-0127",
  # Owner: geir-gronmo
  # Added: 2026-05-22
  # Review-By: 2026-08-22
  # Upstream: https://github.com/rust-random/rand/issues/1514
  # Remove-When: sqlx 0.8.x updates to rand >=0.9.3
  # rand 0.8.5 is pulled in by sqlx-postgres v0.8.x (dev-only path). The unsoundness
  # requires a custom logger + specific log feature interaction. sqlx 0.8.x does not
  # expose a patched 0.8.x; resolved in rand >=0.9.3 (our non-sqlx deps are updated).
  "RUSTSEC-2026-0097",
  # Owner: geir-gronmo
  # Added: 2026-05-22
  # Review-By: 2026-08-22
  # Upstream: https://github.com/rustls/webpki/issues/320
  # Remove-When: upstream updates to rustls-webpki >=0.103.13
  # rustls-webpki reachable panic in CRL parsing. Pulled in transitively via
  # ureq (dev / test path). We do not parse CRLs; unaffected in practice.
  # Will be resolved when upstream updates to rustls-webpki >=0.103.13.
  "RUSTSEC-2026-0104",
]

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"
# These duplicate versions come from pgrx pulling one version and
# testcontainers/bollard pulling another. We have no direct control over these
# version skews; they are dev-only or cross-compilation stubs.
skip = [
  # thiserror: pgrx uses v1, our code and testcontainers use v2
  { name = "thiserror",      version = "=1.0.69" },
  { name = "thiserror-impl", version = "=1.0.69" },
  # getrandom: split between pgrx (0.3) and newer crates (0.4)
  { name = "getrandom", version = "=0.3.4" },
  # hashbrown: multiple major versions pulled by indexmap and others
  { name = "hashbrown", version = "=0.15.5" },
  # foldhash: 0.1.x from hashbrown (via pgrx/petgraph) vs 0.2.x from newer hashbrown (via sqlx)
  { name = "foldhash", version = "=0.1.5" },
  # wasi: 0.11 from older crates, 0.14 from newer wasmtime-based stack
  { name = "wasi", version = "=0.11.1+wasi-snapshot-preview1" },
  # windows-* crates: version skew between ring (0.52) and socket2/hyper-util
  # (0.53+) pulled by bollard/testcontainers
  { name = "windows-core",      version = "=0.57.0" },
  { name = "windows-implement", version = "=0.57.0" },
  { name = "windows-interface", version = "=0.57.0" },
  { name = "windows-result",    version = "=0.1.2"  },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
