# The Relay Deep Dive: NATS, Redis Streams, and RabbitMQ

This article describes relay backends that moved out of `pg_trickle`. The relay
is now maintained by the [`pg_tide` project](https://github.com/trickle-labs/pg-tide), which owns its current implementation and operator documentation.
