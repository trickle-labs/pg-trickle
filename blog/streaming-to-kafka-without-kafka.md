# Streaming to Kafka Without Kafka Expertise

This article uses the former `pgtrickle-relay` APIs and configuration. The
relay now belongs to the [`pg_tide` project](https://github.com/trickle-labs/pg-tide).
For current `pg_trickle` outbox integration guidance, see the [Outbox documentation](https://github.com/trickle-labs/pg-trickle/blob/main/docs/OUTBOX.md).
